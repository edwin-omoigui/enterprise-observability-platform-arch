apiVersion: v1
kind: Service
metadata:
  name: sample-otel-app
  labels:
    app: sample-otel-app
spec:
  selector:
    app: sample-otel-app
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
